import { useState } from 'react';import { MapRoute } from './routes/map';import { Preferences } from './routes/index';import { Designs } from './routes/designs';
export function App(){const [route,setRoute]=useState('map');return <><header><div className="logo">EDGE<span>WISE</span></div><nav><button className={route==='map'?'on':''} onClick={()=>setRoute('map')}>1 Map</button><button className={route==='preferences'?'on':''} onClick={()=>setRoute('preferences')}>2 Preferences</button><button className={route==='designs'?'on':''} onClick={()=>setRoute('designs')}>3 Designs</button></nav></header>{route==='map'?<MapRoute go={setRoute}/>:route==='preferences'?<Preferences go={setRoute}/>:<Designs go={setRoute}/>}</>}
